[project]
name = "election-problem"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
]

[tool.vercel]
entrypoint = "app:app"
